||when pods restart; data inside pod get lose
||works as attached directory to pod
||volumes can share with several containers

||emptyDir -> temporary storage shared between containers in a pod
              (when pod get deleted, volume get deleted)


>kubectl apply -f emptydir_share.yml


||via writer container; write a file
||via reader container; check whether it can be read

||go inside writer container
>kubectl exec -it emptydir-share-demo -c writer -- sh

||write a file
>echo "This fiel is shared" > /data/shared.txt

||go inside reader container
>kubectl exec -it emptydir-share-demo -c reader -- cat /data/shared.txt

||delete pod
||recreate pod
||check emptyDir volume exist